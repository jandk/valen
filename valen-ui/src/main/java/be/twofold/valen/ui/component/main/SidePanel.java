package be.twofold.valen.ui.component.main;

/**
 * Which panel occupies the side of the main split pane, if any. Preview and
 * settings are mutually exclusive.
 */
public enum SidePanel {
    NONE,
    PREVIEW,
    SETTINGS
}
